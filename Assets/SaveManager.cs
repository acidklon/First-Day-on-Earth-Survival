using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using YG;

namespace Watermelon
{
    public class SaveManager : MonoBehaviour
    {
        /*public static SaveManager Instance;

        private static GlobalSave globalSave;

        private void Awake()
        {
            Instance = this;
        }

        private void Start()
        {
            
        }

        public void Load(float time)
        {
            globalSave = BaseSaveWrapper.ActiveWrapper.Load("save");
            globalSave.Init(time);
        }

        public void Save()
        {
            SavesYG.SetString("SaveMy", globalSave.ObjectToString());
        }*/
    }
}